# รายงานตรวจสอบความสมบูรณ์ — TonPao Map (Public Prototype)

วันที่ตรวจ: 24 ก.ย. 2569 · คอมมิตที่ตรวจ: `89e0800` · ขอบเขต: โฟลเดอร์ `dist/` ทั้งหมด

## 1. สรุปผู้บริหาร

| หัวข้อ | สถานะ | หมายเหตุ |
|---|---|---|
| ครบทุก route ตามที่ประกาศใน `router.js` | ✅ ผ่าน | 39 route เปิดได้ทั้งหมด ไม่มีไฟล์หาย (ไม่มี 404 ภายในเว็บ) |
| JavaScript ของเว็บเองไม่มี error | ✅ ผ่าน | error ที่พบมาจากสภาพแวดล้อมทดสอบ (บล็อก CDN) ไม่ใช่โค้ดของเว็บ |
| Responsive (desktop 1366px / mobile 390px) | ✅ ผ่านเบื้องต้น | ไม่มี horizontal scroll ทุกหน้า มีเมนูล่างบนมือถือ |
| แผนที่ (Leaflet) | ✅ ทำงาน | โหลดได้ 13 หน้า (สำรวจ, หมวดหมู่ 5 หน้า, กิจกรรม, ที่จอดรถ, แผนเที่ยว, dashboard ผู้ร่วมให้ข้อมูล) |
| ส่วนผู้ร่วมให้ข้อมูล (Contributor) | ✅ สมบูรณ์ที่สุด | 13 view, ปุ่ม 106 ปุ่มตอบสนองทั้งหมด (0 ปุ่มตาย) |
| ปุ่ม/ลิงก์ฝั่งสาธารณะ | ⚠️ บางส่วน | 599 ปุ่ม: ใช้งานได้ ~77% · ไม่ตอบสนอง 120 ปุ่ม (~20%) |
| บั๊กการนำทาง | ✅ แก้แล้ว (ข้อ 3.1–3.4) | เหลือข้อ 3.5–3.6 ระดับเล็ก |
| ความพร้อมเป็นเว็บแอป production | ❌ ยังไม่พร้อม | เป็นต้นแบบ (prototype) ข้อมูล hard-code ไม่มี backend/PWA/ซอร์สโค้ด build |

**ข้อสรุป:** เป็น *ต้นแบบหน้าจอที่คลิกได้ (clickable prototype)* ที่ครบทุกหน้าจอและนำทางถึงกันได้เกือบทั้งหมด เหมาะสำหรับสาธิต/ทดสอบกับผู้ใช้ แต่ยัง **ไม่ใช่เว็บแอปพร้อมใช้งานจริง**

## 2. วิธีตรวจ

- เปิดเว็บด้วย `http-server` แล้วใช้ Playwright (Chromium) เปิดทุก route ทั้ง desktop (1366×900) และ mobile (390×844, touch) รวม 82 หน้าจอ เก็บ console error, request ที่ล้มเหลว, รูปเสีย, horizontal overflow และภาพหน้าจอ
- คลิกทดสอบทุกปุ่ม/ลิงก์ที่มองเห็นได้ (705 รายการ บน 36 route) และบันทึกว่าไปหน้าใหม่ / เปลี่ยน UI / ไม่มีผล
- ตรวจแบบ static: ลิงก์ภายใน, ไฟล์ที่อ้างถึง, dependency ภายนอก, ความสอดคล้องของข้อมูล

**ข้อจำกัด:** sandbox บล็อก `cdn.tailwindcss.com` และ `unpkg.com` จึงแทนด้วย Tailwind v4 browser build และ Leaflet 1.9.4 จาก npm — **หน้าตา (สี/ฟอนต์/ระยะ) ของหน้าที่ใช้ Tailwind จึงไม่ตรงของจริง** และไม่ได้ทดสอบ tile แผนที่จริง ผลด้านฟังก์ชัน/การนำทางยังเชื่อถือได้

## 3. บั๊กที่ควรแก้ (เรียงตามความสำคัญ)

> **อัปเดต:** ข้อ 1–4 แก้แล้วใน branch `claude/stoic-mayer-79z9lw` — `prototype-nav.js` ให้ลิงก์ที่มี `href="/#…"` ชัดเจนทำงานก่อนการตรวจคำว่า "กลับ" (แก้โลโก้ และปุ่ม "กลับหน้าแรก" ในหน้า 500 ด้วย), ผูก `[data-edit-itinerary]` ไป `/plan/build`, ผูกปุ่มเข้าสู่ระบบในแถบหัวไป `/contributor/login` และใส่ route จริงให้ลิงก์ในหน้า 404

1. **คลิกโลโก้ "TonPao Map" แล้วถอยกลับหน้าก่อน แทนที่จะไปหน้าแรก** (ทุกหน้า)
   `prototype-nav.js` ตั้ง `aria-label="กลับหน้าแรก TonPao Map"` ให้โลโก้ แล้ว click handler ตัวเดียวกันตีความทุกปุ่มที่ aria-label มีคำว่า "กลับ" เป็นปุ่มย้อนกลับ → เรียก `history.back()` ถ้าผู้ใช้เข้าเว็บตรงมาจากลิงก์ภายนอก การคลิกโลโก้จะ **ออกจากเว็บไป**
2. **ปุ่ม "ปรับเงื่อนไข" หน้าผลแผนเที่ยว (`/plan/result`) ถอยกลับแทนไปหน้า `/plan/build`** — สาเหตุเดียวกัน (`aria-label="กลับไปปรับเงื่อนไข…"`) handler ระดับ document แบบ capture ทำงานก่อน handler ของปุ่มใน `desktop-prototype.js:453`
3. **หน้า 404 ลิงก์กู้คืนใช้ไม่ได้** — "กลับสู่หน้าหลัก", "สำรวจแผนที่", "ติดต่อเรา", "เข้าสู่ระบบ" เป็น `href="#"` ทั้งหมด (`screens/s17/desktop.html`)
4. ปุ่ม **"เข้าสู่ระบบ" / "Sign In"** ในแถบหัวของหน้าสาธารณะไม่ทำอะไร (ควรไป `/#/contributor/login`)
5. ลิงก์ค้างจากชื่อไฟล์เก่า `href="S-05"`, `href="S-04"` ใน `screens/s06/desktop.html` (ถูก handler ดักตามข้อความจึงยังไม่พังตอนนี้ แต่เปราะบาง)
6. `router.js` `currentRoute()` เขียน `routes[value] || /regex/.test(value) ? value : '/404'` — ทำงานถูกโดยบังเอิญจาก operator precedence ควรใส่วงเล็บให้ชัด

## 4. ปุ่มที่ยังไม่มีการทำงาน (placeholder)

120 ปุ่มฝั่งสาธารณะ ส่วนใหญ่เป็นฟีเจอร์ที่ยังไม่ทำ:

- ชิปกรองหมวดย่อยในหน้าหมวดหมู่ 5 หน้า (เช่น ร่มบ่อสร้าง/กระดาษสา/งานไม้), ปุ่ม bookmark, ปุ่มเลขหน้า 1/2/›
- หน้า POI: โทร, เว็บไซต์, Facebook, แชร์, บันทึก, "ดูทั้งหมด"
- ปุ่ม help / notifications / mail ในแถบหัว
- หน้าช่วยเหลือ: ช่องค้นหา, "ติดต่อฝ่ายสนับสนุน"; หน้าเกี่ยวกับ: "ส่งข้อความถึงเรา"
- ตารางกิจกรรม: ปุ่มเลือกวัน 20/21 มกราคม **2567** (ปีเก่า ขณะที่ส่วนอื่นใช้ 2569)
- แผนเที่ยว: แชร์, ปุ่มสลับลำดับ ↓, "ดำเนินการต่อ" ใน `/my-itineraries`
- หมุด (marker) บางตัวในแผนที่ไม่ตอบสนองเมื่อคลิกในการทดสอบอัตโนมัติ (ควรตรวจซ้ำด้วยมือ)

## 5. ความพร้อมเป็นเว็บแอป

| รายการ | สถานะ |
|---|---|
| Hosting แบบ static (`.openai/hosting.json` → `dist`) | มี · ใช้ path แบบ absolute (`/router.js`, `/responsive-shell.css`) ต้อง deploy ที่ root ของโดเมน (เช่น GitHub Pages แบบ project path จะพัง) |
| ซอร์สโค้ด / `package.json` / ขั้นตอน build | ❌ ไม่มี — repo มีแค่ไฟล์ output, README ถูกลบในคอมมิต import |
| Tailwind | ใช้ Play CDN (`cdn.tailwindcss.com`) ที่ Tailwind ระบุว่า **ไม่ใช่สำหรับ production** (คอมไพล์ในเบราว์เซอร์ทุกครั้ง ช้า และล่มตาม CDN) |
| Dependency ภายนอกขณะรันไทม์ | unpkg (Leaflet), Google Fonts, OSM tiles, รูปจาก `lh3.googleusercontent.com` 122 จุด (URL ที่ AI/Stitch สร้าง อาจหมดอายุ) |
| PWA (manifest, service worker, ไอคอน) | ❌ ไม่มี · หน้า "ออฟไลน์" (`/offline`) เป็นแค่หน้าจอตัวอย่าง ใช้งานออฟไลน์จริงไม่ได้ |
| favicon | ❌ ไม่มี |
| ข้อมูล | hard-code ใน JS · สถานะเก็บใน `localStorage` เท่านั้น · ไม่มี API/backend |
| การยืนยันตัวตน Contributor | mock (อีเมล/รหัสผ่านตัวอย่างเติมไว้ในฟอร์ม) |
| Routing | hash router + iframe (URL เป็น `/#/…`, SEO/แชร์ลิงก์ไม่ดี, ปุ่ม back ซ้อน) |
| ไฟล์ `screens/*/mobile.html` | ไม่ถูกใช้แล้ว (router ใช้ `desktop.html` ทุกขนาดจอ) เหลือไว้เป็น reference 23 ไฟล์ |
| การทดสอบ / CI / LICENSE | ❌ ไม่มี |

### ความสอดคล้องของข้อมูล
- "ศูนย์อุตสาหกรรมทำร่มบ่อสร้าง" มีพิกัดไม่ตรงกัน: แผนที่สาธารณะ `18.7682, 99.1195` vs ส่วน Contributor `18.7622, 99.0751` (ห่างกัน ~4.7 กม.) — ควรยืนยันพิกัดจริง
- วันที่ในตารางกิจกรรมเป็นปี 2567

## 6. ข้อเสนอแนะ

**ระยะสั้น (แก้ได้ทันที):** ~~แก้บั๊กข้อ 3.1–3.4~~ (แก้แล้ว), เพิ่ม favicon, คืน README อธิบายโครงสร้างและ route

**ระยะกลาง (ก่อนเปิดใช้จริง):** ย้ายเป็นโปรเจกต์มีซอร์ส + build (เช่น Vite) คอมไพล์ Tailwind ตอน build, bundle Leaflet และฟอนต์เอง, เก็บรูปไว้ใน repo, เลิกใช้ iframe เปลี่ยนเป็น router ปกติ, เพิ่ม manifest + service worker ให้ใช้ออฟไลน์ได้จริง

**ระยะยาว:** backend/API สำหรับ POI, กิจกรรม, บัญชี Contributor และขั้นตอน Moderator; ระบบยืนยันตัวตนจริง; ทดสอบอัตโนมัติใน CI
